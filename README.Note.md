# Remote

```bash

    >- git remote

    # 단축 이름과 URL
    >- git remote -v

    # 저장소 추가
    >- git remote add rgit git@github.com:ViVaKR/Git.git

    $ git remote show origin

    $ git push < 리모트저장소이름 > < 브랜치이름 >


    ## Alias

    $ git config


```
